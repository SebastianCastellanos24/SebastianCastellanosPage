export const Header = (props) => {

    return (
        <header>
            <div className="flex intems-center justify-between">
                <img src="/react.svg" width={220} height={55} />
            </div>
            
        </header>
    )
    
}